# SB Stocks - Paper Trading Simulation App

A full-stack stock trading simulation web application built with React (Vite) + Express.js + MongoDB.

## Setup

### Client
```bash
cd client
npm install
npm run dev
```

### Server
```bash
cd server
npm install
nodemon index.js
```